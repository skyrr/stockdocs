<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorThemePrimary</item>
        <item name="colorPrimaryDark">@color/colorThemePrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="textColorPrimary">@android:color/white</item>-->
        <!--<item name="android:colorBackground">@android:color/background_light</item>-->
        <!--<item name="android:navigationBarColor">@color/colorThemePrimaryDark</item>-->
    </style>

</resources>
